import React, {useEffect, useState} from 'react';
import {connect} from 'react-redux'
import {
    SafeAreaView,
    StyleSheet,
    ScrollView,
    View,
    Text,
    StatusBar,
    FlatList
  } from 'react-native';
  

const ViewTechnicianEventScreen = (state) => {
    return (
        <View>
            <Text>test</Text>
        </View>
    );
};


export default connect(null, null)(ViewTechnicianEventScreen);